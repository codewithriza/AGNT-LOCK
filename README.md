# AGNT-LOCK
It's a "Context Traffic Controller" for your repository. It prevents multiple AI agents (like Claude Code, and Aider) from stepping on each other's toes by creating a standardized Lock &amp; Sync protocol.
